import lib

use_example = True
input_text = lib.read_input(__file__, use_example)

# For each range pair, determine if one is wholly contained by the other

range_pairs = input_text.splitlines()
answer = 0
print(answer)
